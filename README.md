
  # Sistema de control de inventario

  This is a code bundle for Sistema de control de inventario. The original project is available at https://www.figma.com/design/c3bm1LO4yzD7uOs1DjOhf0/Sistema-de-control-de-inventario.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  